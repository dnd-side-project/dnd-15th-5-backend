dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
    }
}

rootProject.name = "chopchop-backend"

include(
        "app-server",
        "module-account",
        "module-receipt",
        "module-place",
        "module-core"
)
