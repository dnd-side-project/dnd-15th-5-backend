dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
    }
}

rootProject.name = "chapchap-backend"

include(
        "app-server",
        "module-account",
        "module-consumption",
        "module-report",
        "module-place",
        "module-core"
)
